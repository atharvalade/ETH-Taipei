{
  "name": "authentica-blockchain",
  "version": "1.0.0",
  "description": "Blockchain contracts for Authentica using Rootstock and Hyperlane",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:rootstock:testnet": "hardhat run scripts/deploy-rootstock.js --network rootstock-testnet",
    "deploy:worldchain:testnet": "hardhat run scripts/deploy-worldchain.js --network worldchain-testnet",
    "events:listen": "node scripts/event-listener.js"
  },
  "keywords": [
    "authentica",
    "rootstock",
    "hyperlane",
    "blockchain"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "hardhat": "^2.17.0"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "dotenv": "^16.0.3",
    "ethers": "^5.7.2"
  }
} 